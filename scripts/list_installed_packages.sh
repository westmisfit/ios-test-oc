set -x
pip --version
easy_install --version
brew --version
python --version
ruby --version
pod --version
mvn --version
gem --version
rvm --version
bundle --version
xcodebuild --version
setuptools --version
true
